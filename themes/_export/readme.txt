When you modify a style file :
  - you can point to an external resource, then use URL of the resource as usual.
  - you can point to an internal (in the zip archive) resource, then use [archive] to point a resource inside the archive like: [archive]/img/my_picture.png
